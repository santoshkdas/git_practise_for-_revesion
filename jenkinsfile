pipeline{
    agent any
    options{
        timestamps()
        buildDiscarder(logRotator(numToKeepStr:'3'))
    }
    stages{
        stage("sample code"){
            steps{
                echo "hello "
                sleep(5)
                echo "hi"
                sleep(5)
                echo "this is sample pipeline script"
            }
        }
    }
}